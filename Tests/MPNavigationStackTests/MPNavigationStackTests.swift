import XCTest
@testable import MPNavigationStack

final class MPNavigationStackTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MPNavigationStack().text, "Hello, World!")
    }
}
